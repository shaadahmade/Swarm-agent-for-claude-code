# Shell scripts and the node installer must keep LF endings. CRLF breaks the
# shebang on Linux and macOS with "bad interpreter: /usr/bin/env bash^M".
*.sh text eol=lf
*.js text eol=lf
*.md text eol=lf
*.json text eol=lf
LICENSE text eol=lf
.gitignore text eol=lf
